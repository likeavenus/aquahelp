export default function advantages() {
    const slider = $('.owl-carousel');

    slider.owlCarousel({
        responsive: {
            0: {
                items: 1
            }
        }
    });
}